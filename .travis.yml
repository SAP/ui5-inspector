language: node_js
sudo: false
node_js:
  - "0.12"

before_install:
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

services:
    - xvfb

before_script:
  - npm install -g grunt-cli

script:
  - grunt travis
